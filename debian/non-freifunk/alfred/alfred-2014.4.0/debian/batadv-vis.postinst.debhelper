# Automatically added by dh_installinit
if [ -x "/etc/init.d/batadv-vis" ]; then
	update-rc.d batadv-vis defaults >/dev/null
	invoke-rc.d batadv-vis start || exit $?
fi
# End automatically added section
